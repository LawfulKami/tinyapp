<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
   <div>
     <% if (!user) { %> 
    <form>
      <button type="submit" class="btn btn-primary" formmethod="GET" formaction="/register">Register</button>
      <button type="submit" class="btn btn-primary" formmethod="GET" formaction="/login">Sign in</button>
    </form>
      <% } else { %>
        <form method="POST" action="/logout">
          <h4 class="navbar-brand"><%= user.email %> 
          <button class="btn btn-primary" type="submit" style="margin-left: 1em;">Log out</button>
          </h4>
        </form>
      <% } %>
    </div>
  </nav>
</header>